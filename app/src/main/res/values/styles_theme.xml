<?xml version="1.0" encoding="utf-8"?>

<!-- Styles applied over specific app components (composed into theme in themes.xml) -->
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Action Bar -->
    <style name="ActionBarAppTheme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/action_bar</item>
        <item name="background">@color/action_bar</item> <!-- Support library compatibility -->

        <item name="android:titleTextStyle">@style/ActionBarAppTheme.TitleTextStyle</item>
        <item name="titleTextStyle">@style/ActionBarAppTheme.TitleTextStyle</item> <!-- Support library compatibility -->

        <item name="android:displayOptions">showHome|homeAsUp|showTitle</item>
        <item name="displayOptions">showHome|homeAsUp|showTitle</item> <!-- Support library compatibility -->

        <!-- Hide app icon -->
        <item name="android:icon">@android:color/transparent</item>
        <item name="icon">@android:color/transparent</item> <!-- Support library compatibility -->
    </style>

    <!-- Action Bar Popup (off screen options) -->
    <style name="ActionBarPopupAppTheme" parent="@style/ThemeOverlay.AppCompat.Light">
        <item name="android:background">@android:color/white</item>
        <item name="background">@android:color/white</item> <!-- Support library compatibility -->
        <item name="android:textColor">@color/action_bar</item>
    </style>

    <!-- Action Bar arrow -->
    <style name="ActionBarAppTheme.DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@android:color/white</item>
    </style>

    <!-- Action Bar text -->
    <style name="ActionBarAppTheme.TitleTextStyle" parent="@style/TextAppearance.AppCompat">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>